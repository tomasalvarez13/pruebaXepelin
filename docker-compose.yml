version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: xepelin
      POSTGRES_PASSWORD: xepelin
      POSTGRES_DB: xepelin_crm
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
